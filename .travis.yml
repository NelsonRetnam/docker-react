sudo: required
language: generic
install: travis_wait mvn install
services:
  - docker

before_install:
  - docker build -t nelsonr/docker-react -f Dockerfile.dev .

script:
  - docker run nelsonr/docker-react npm run test -- --coverage